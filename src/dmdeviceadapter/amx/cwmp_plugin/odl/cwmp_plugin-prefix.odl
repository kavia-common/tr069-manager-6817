%config {
   prefix = "";
   cfg-dir = "/etc/amx";
   include-dirs = [
      "${prefix}${cfg-dir}/modules"
   ];
}

#include "global_variables.odl";
%config {
   %global vendor_prefix = "${global_vendor_prefix_}";
}
